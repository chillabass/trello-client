import { Button } from '@mui/material';
import React from 'react';

export const ProfileDesks: React.FC = () => {
  return (
    <>
      <Button variant='outlined'>Create desk!</Button>
    </>
  )
};
